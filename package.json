{
  "name": "agent-plugins",
  "type": "module",
  "private": true,
  "scripts": {
    "pack": "bun run --filter=* pack",
    "validate": "bun run --filter=* validate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "cursor",
    "claude-desktop"
  ]
}
